<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.svenruppert</groupId>
        <artifactId>dependencies</artifactId>
        <version>06.02.05</version>
        <!-- Resolved from the repository, not from ../pom.xml. -->
        <relativePath/>
    </parent>

    <artifactId>vaadinapp</artifactId>
    <version>00.01.00</version>
    <packaging>pom</packaging>

    <!--
        Reactor for the article series "Vaadin - Deployment auf Hetzner".

        One module per delivery form, so that each part of the series has a place
        where its variant lives and nothing bleeds between them:

          core            the application itself, plus the Vaadin production bundle
          war-jetty       WAR for an externally installed Jetty   (parts 1 and 2)
          embedded-jetty  own main() with an embedded Jetty       (part 3)

        Until part 3 this was a single module that built both forms - the WAR by
        default and a fat jar through a _shadejar profile. That double role was
        the root cause of T1-A04: the embedded launcher lived in src/main/java and
        was therefore compiled into every build, dragging the whole Jetty stack
        into the WAR. Separate modules remove the cause instead of patching it.

        The state the finished parts 1 and 2 describe is preserved as tag teil-02.
    -->

    <inceptionYear>2026</inceptionYear>
    <organization>
        <name>Sven Ruppert</name>
        <url>https://svenruppert.com</url>
    </organization>

    <url>https://github.com/svenruppert/Blog-Vaadin-Deployment-on-Hetzner</url>
    <name>Vaadin Deployment on Hetzner — Demo Application</name>
    <description>The Vaadin Flow application deployed throughout the "Vaadin - Deployment on Hetzner" article series: one module per delivery form, one tag per part.</description>
    <scm>
        <url>https://github.com/svenruppert/Blog-Vaadin-Deployment-on-Hetzner</url>
        <connection>scm:git:https://github.com/svenruppert/Blog-Vaadin-Deployment-on-Hetzner.git</connection>
        <developerConnection>scm:git:https://github.com/svenruppert/Blog-Vaadin-Deployment-on-Hetzner.git
        </developerConnection>
        <tag>HEAD</tag>
    </scm>
    <developers>
        <developer>
            <name>Sven Ruppert</name>
            <email>sven.ruppert@gmail.com</email>
            <roles>
                <role>Founder</role>
                <role>Developer</role>
            </roles>
            <timezone>+1</timezone>
        </developer>
    </developers>
    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/svenruppert/Blog-Vaadin-Deployment-on-Hetzner/issues</url>
    </issueManagement>
    <licenses>
        <license>
            <name>European Union Public Licence 1.2</name>
        </license>
    </licenses>

    <modules>
        <module>core</module>
        <module>war-jetty</module>
        <module>embedded-jetty</module>
    </modules>

    <properties>
        <licenseName>European Union Public Licence 1.2</licenseName>

        <deploy-repo-url>https://repo.maven.apache.org/maven2/</deploy-repo-url>
        <deploy-repo-snapshots-url>https://repo.maven.apache.org/maven2/</deploy-repo-snapshots-url>

        <jdk.version>26</jdk.version>

        <maven.compiler.release>${jdk.version}</maven.compiler.release>
        <maven.compiler.source>${jdk.version}</maven.compiler.source> <!--redundant, but IntelliJ doesn't-->
        <maven.compiler.target>${jdk.version}</maven.compiler.target> <!--use release when importing-->

        <maven.compiler.testRelease>${jdk.version}</maven.compiler.testRelease>
        <maven.compiler.testSource>${jdk.version}</maven.compiler.testSource>
        <maven.compiler.testTarget>${jdk.version}</maven.compiler.testTarget>

        <minimum-maven.version>3.9.9</minimum-maven.version>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <pitest-prod-classes>com.svenruppert.*</pitest-prod-classes>
        <pitest-test-classes>junit.com.svenruppert.*</pitest-test-classes>

        <!--        Vaadin Flow Properties-->
        <failOnMissingWebXml>false</failOnMissingWebXml>
        <vaadin.version>25.2.6</vaadin.version>
        <jetty.version>12.1.12</jetty.version>
        <warName>ROOT</warName>
        <slf4j-api.version>2.1.0-alpha1</slf4j-api.version>
        <jcustos.version>00.83.00</jcustos.version>

        <!--
            Not managed by the grandparent POM, unlike shade, jar and dependency.
            Used only by the thin profile of embedded-jetty, but pinned here so
            the reactor keeps one place for plugin versions.
        -->
        <maven-assembly-plugin.version>3.7.1</maven-assembly-plugin.version>

        <!--
            Jetty's annotation scanner reads class files with ASM. Default
            resolution mixes asm 9.8 (from j2objc-annotations) with asm-tree and
            asm-commons 9.9.1 (from Jetty); that mix crashes on Java-26 bytecode
            (class-file major version 70) with "Unsupported class file major
            version 70" and the WebAppContext fails to start. Pin all four
            modules to one version that knows CF 70. Only the embedded module
            scans, but the property belongs here so the two cannot drift.
        -->
        <asm.version>9.10.1</asm.version>

        <!--
            Filters and scripts live in the reactor root, not in the modules.
            Without this the module-relative paths break after the split.
        -->
        <tools.dir>${maven.multiModuleProjectDirectory}/tools</tools.dir>
    </properties>

    <repositories>
        <!-- The order of definitions matters. Explicitly defining central here to make sure it has the highest priority. -->
        <repository>
            <id>central</id>
            <url>https://repo.maven.apache.org/maven2</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>vaadin-prereleases</id>
            <url>
                https://maven.vaadin.com/vaadin-prereleases/
            </url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <!-- Repository used by many Vaadin add-ons -->
        <repository>
            <id>Vaadin Directory</id>
            <url>https://maven.vaadin.com/vaadin-addons</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>apache snapshots</id>
            <url>https://repository.apache.org/content/repositories/snapshots</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <!-- The order of definitions matters. Explicitly defining central here to make sure it has the highest priority. -->
        <pluginRepository>
            <id>central</id>
            <url>https://repo.maven.apache.org/maven2</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
        <pluginRepository>
            <id>vaadin-prereleases</id>
            <url>
                https://maven.vaadin.com/vaadin-prereleases/
            </url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-bom</artifactId>
                <type>pom</type>
                <scope>import</scope>
                <version>${vaadin.version}</version>
            </dependency>
            <!--
                Both Jetty BOMs make ${jetty.version} the single source of truth for
                core and ee11 modules alike - the WAR module compiles against the
                servlet API only, the embedded module against the server itself, and
                neither may drift from the Jetty the series documents.
            -->
            <dependency>
                <groupId>org.eclipse.jetty</groupId>
                <artifactId>jetty-bom</artifactId>
                <type>pom</type>
                <scope>import</scope>
                <version>${jetty.version}</version>
            </dependency>
            <dependency>
                <groupId>org.eclipse.jetty.ee11</groupId>
                <artifactId>jetty-ee11-bom</artifactId>
                <type>pom</type>
                <scope>import</scope>
                <version>${jetty.version}</version>
            </dependency>

            <!--
                The reactor's own modules, so children can omit the version.
                Note the artifactId prefix: a module simply called "core" would
                collide with com.svenruppert:core, which this application depends on.
            -->
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>vaadinapp-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>vaadinapp-core</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>

            <dependency>
                <groupId>jakarta.servlet</groupId>
                <artifactId>jakarta.servlet-api</artifactId>
                <version>6.1.0</version>
            </dependency>

            <!--
                Pin the SLF4J family. Without this log4j-over-slf4j drifts to
                2.0.17 in the packaging modules through transitive mediation,
                while core resolves 2.1.0-alpha1 - two versions of the same
                bridge in one reactor.
            -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j-api.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>${slf4j-api.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>log4j-over-slf4j</artifactId>
                <version>${slf4j-api.version}</version>
            </dependency>

            <dependency>
                <groupId>org.ow2.asm</groupId>
                <artifactId>asm</artifactId>
                <version>${asm.version}</version>
            </dependency>
            <dependency>
                <groupId>org.ow2.asm</groupId>
                <artifactId>asm-tree</artifactId>
                <version>${asm.version}</version>
            </dependency>
            <dependency>
                <groupId>org.ow2.asm</groupId>
                <artifactId>asm-commons</artifactId>
                <version>${asm.version}</version>
            </dependency>
            <dependency>
                <groupId>org.ow2.asm</groupId>
                <artifactId>asm-analysis</artifactId>
                <version>${asm.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <inherited>true</inherited>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <executions>
                    <execution>
                        <id>enforce-maven-3</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireMavenVersion>
                                    <version>${minimum-maven.version}</version>
                                </requireMavenVersion>
                            </rules>
                            <fail>true</fail>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--
                Declared here so the "jetty" plugin prefix resolves in every module.
                The run mojo supports war packaging only and skips itself elsewhere,
                which makes `./mvnw -pl war-jetty -am package jetty:run-war` work from
                the reactor root.
            -->
            <plugin>
                <groupId>org.eclipse.jetty.ee11</groupId>
                <artifactId>jetty-ee11-maven-plugin</artifactId>
                <version>${jetty.version}</version>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-scm-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
            </plugin>

            <!--
                Turns off the license header writer the parent POM binds to
                process-sources. Must stay in the reactor parent so it reaches
                every module - otherwise headers reappear in all sources.
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>first</id>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>

            <!--
                makeAggregateBom walks the whole reactor, so it belongs to the
                aggregator exactly once. inherited=false keeps it from running
                again in every module - in the single-module layout it ran twice,
                because the parent POM binds an execution of its own.
            -->
            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
                <version>2.9.3</version>
                <executions>
                    <!--
                        Switch off the execution the grandparent POM binds in every
                        module. inherited=false on the plugin would only silence the
                        root; the override has to reach the children, so it lives on
                        the execution.
                    -->
                    <execution>
                        <id>build-sbom</id>
                        <phase>none</phase>
                    </execution>
                    <!-- The aggregate BOM, once, in the aggregator. -->
                    <execution>
                        <id>aggregate-sbom</id>
                        <inherited>false</inherited>
                        <phase>package</phase>
                        <goals>
                            <goal>makeAggregateBom</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <projectType>library</projectType>
                    <schemaVersion>1.6</schemaVersion>
                    <includeBomSerialNumber>true</includeBomSerialNumber>
                    <includeCompileScope>true</includeCompileScope>
                    <includeProvidedScope>true</includeProvidedScope>
                    <includeRuntimeScope>true</includeRuntimeScope>
                    <includeSystemScope>true</includeSystemScope>
                    <includeTestScope>false</includeTestScope>
                    <includeLicenseText>false</includeLicenseText>
                    <outputReactorProjects>true</outputReactorProjects>
                    <outputName>CycloneDX-SBom</outputName>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
