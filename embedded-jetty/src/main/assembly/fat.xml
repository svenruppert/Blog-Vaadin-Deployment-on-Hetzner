<?xml version="1.0" encoding="UTF-8"?>
<!--
    The fat jar as a release, laid out exactly like the thin distribution:

        bin/    the same start script, byte for byte
        app/    the shaded archive
        (no lib/)

    That symmetry is the point. bin/start.sh puts "$APP_HOME/app/*:$APP_HOME/lib/*"
    on the classpath; with no lib/ directory the second half expands to nothing,
    and the single complete archive in app/ is all the scanner needs - the very
    property that makes a fat jar work at all.

    The consequence on the server: both forms are releases of identical shape,
    the systemd unit never changes between them, and switching from one to the
    other is a symlink and a restart. Which is what makes them comparable.
-->
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
                              http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>fat</id>

    <formats>
        <format>dir</format>
        <format>tar.gz</format>
    </formats>

    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>vaadinapp-${project.version}</baseDirectory>

    <fileSets>
        <fileSet>
            <directory>${project.basedir}/src/main/dist/bin</directory>
            <outputDirectory>bin</outputDirectory>
            <fileMode>0755</fileMode>
        </fileSet>
    </fileSets>

    <files>
        <!--
            The bill of materials travels with the release.

            Part 4 leans on it twice - it is the only remaining answer to "which
            version of a library is in there?" once a fat jar has merged them all.
            An SBOM that stays in target/ on the build machine cannot answer that
            question on the machine where it gets asked.
        -->
        <file>
            <source>${project.build.directory}/sbom.json</source>
            <outputDirectory>.</outputDirectory>
            <fileMode>0644</fileMode>
        </file>
        <file>
            <source>${project.basedir}/src/main/dist/VERSION</source>
            <outputDirectory>.</outputDirectory>
            <filtered>true</filtered>
            <fileMode>0644</fileMode>
        </file>
        <file>
            <source>${project.build.directory}/vaadinapp-${project.version}-fat.jar</source>
            <outputDirectory>app</outputDirectory>
            <destName>vaadinapp.jar</destName>
            <fileMode>0644</fileMode>
        </file>
    </files>
</assembly>
